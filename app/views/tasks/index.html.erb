<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <!-- &gt; は、 > を HTML で表示させるための記号です。 -->
    <li><%= link_to task.id, task %> : <%= task.status %> &gt; <%= task.content %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの投稿', new_task_path %>